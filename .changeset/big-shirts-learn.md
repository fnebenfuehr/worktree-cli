---
"@fnebenfuehr/worktree-cli": minor
---

Add worktree_checkout command with intelligent branch detection. Automatically discovers branches locally or on remote and creates appropriate worktrees. Switches to existing worktree if branch already checked out. Sets upstream tracking for remote branches.
